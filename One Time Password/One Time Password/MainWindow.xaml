<Window x:Class="One_Time_Password.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:One_Time_Password"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Name="StartButton" Click="StartButton_Click" Content="Start Server" Grid.Column="0"/>
            <Button Name="StopButton" Click="StopButton_Click" Content="Stop Server" Grid.Column="1"/>
        </Grid>

        <ListBox Name="OutputListBox" Height="150"/>
    </StackPanel>
</Window>
